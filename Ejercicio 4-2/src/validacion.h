#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

#ifndef VALIDACION_H_
#define VALIDACION_H_

float ValidarTemperatura(char mensaje[], float min, float max,
		char mensajeError[]);

#endif /* VALIDACION_H_ */
